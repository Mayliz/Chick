<%= form_for @ropa, :html => {:multipart => true} do |f| %>
  <% if @ropa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ropa.errors.count, "error") %> prohibited this ropa from being saved:</h2>

      <ul>
      <% @ropa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
